[project]
name = "webaf"
version = "0.4.0"
description = "Modular Web Application Assessment Framework"
requires-python = ">=3.11"

dependencies = [
    "requests",
    "beautifulsoup4",
    "rich",
    "jinja2",
]
